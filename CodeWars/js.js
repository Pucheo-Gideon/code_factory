// import flipRandomData from "./flipRandomData" assert {type: 'json'};

// import fs from 'fs'

// const storage = JSON.parse(fs.readFileSync("./flipRandomData.json"));
const loopStorage = Math.floor(Math.random() *storage)
console.log(loopStorage)