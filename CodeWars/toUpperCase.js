function makeUpperCase(str) {
  return str.toUpperCase()
}

console.log(makeUpperCase("giDeon"))

// This is what a  human being can do?. Omoh! Amkpa idem . Uwuoh!

function makeUpperCase(str) {
  //return str.toUpperCase()
  let a = "A";
  let b = "B";
  let c = "C";
  let d = "D";
  let e = "E";
  let f = "F";
  let g = "G";
  let h = "H";
  let ii = "I";
  let j = "J";
  let k = "K";
  let l = "L";
  let m = "M";
  let n = "N";
  let o = "O";
  let p = "P";
  let q = "Q";
  let r = "R";
  let s = "S";
  let t = "T";
  let u = "U";
  let v = "V";
  let w = "W";
  let x = "X";
  let y = "Y";
  let z = "Z";

  let strSplit = str.split("");
  let strArray = [];
  let regex = 0;
  let regex1 = 1;
  let regex2 = 2;
  let regex3 = 3;
  let regex4 = 4;
  let regex5 = 5;
  let regex6 = 6;
  let regex7 = 7;
  let regex8 = 8;
  let regex9 = 9;
  for (let i = 0; i < strSplit.length; i++) {
    if (strSplit[i] === "!") {
      strArray.push("!");
    }
    if (strSplit[i] === " ") {
      strArray.push(" ");
    }
    if (strSplit[i] === ",") {
      strArray.push(",");
    }
    if (strSplit[i] === "0") {
      strArray.push(regex);
    }
    if (strSplit[i] === "1") {
      strArray.push(regex1);
    }
    if (strSplit[i] === "2") {
      strArray.push(regex2);
    }
    if (strSplit[i] === "3") {
      strArray.push(regex3);
    }
    if (strSplit[i] === "4") {
      strArray.push(regex4);
    }
    if (strSplit[i] === "5") {
      strArray.push(regex5);
    }

    if (strSplit[i] === "6") {
      strArray.push(regex6);
    }
    if (strSplit[i] === "7") {
      strArray.push(regex7);
    }
    if (strSplit[i] === "8") {
      strArray.push(regex8);
    }
    if (strSplit[i] === "9") {
      strArray.push(regex9);
    }

    if (strSplit[i] === "a") {
      strArray.push(a);
    }
    if (strSplit[i] === "A") {
      strArray.push(a);
    }
    if (strSplit[i] === "b") {
      strArray.push(b);
    }
    if (strSplit[i] === "B") {
      strArray.push(b);
    }
    if (strSplit[i] === "c") {
      strArray.push(c);
    }
    if (strSplit[i] === "C") {
      strArray.push(c);
    }
    if (strSplit[i] === "d") {
      strArray.push(d);
    }
    if (strSplit[i] === "D") {
      strArray.push(d);
    }
    if (strSplit[i] === "e") {
      strArray.push(e);
    }
    if (strSplit[i] === "E") {
      strArray.push(e);
    }
    if (strSplit[i] === "f") {
      strArray.push(f);
    }
    if (strSplit[i] === "F") {
      strArray.push(f);
    }
    if (strSplit[i] === "g") {
      strArray.push(g);
    }
    if (strSplit[i] === "G") {
      strArray.push(g);
    }
    if (strSplit[i] === "h") {
      strArray.push(h);
    }
    if (strSplit[i] === "H") {
      strArray.push(h);
    }
    if (strSplit[i] === "i") {
      strArray.push(ii);
    }
    if (strSplit[i] === "I") {
      strArray.push(ii);
    }
    if (strSplit[i] === "j") {
      strArray.push(j);
    }
    if (strSplit[i] === "J") {
      strArray.push(j);
    }
    if (strSplit[i] === "k") {
      strArray.push(k);
    }
    if (strSplit[i] === "K") {
      strArray.push(k);
    }
    if (strSplit[i] === "l") {
      strArray.push(l);
    }
    if (strSplit[i] === "L") {
      strArray.push(l);
    }
    if (strSplit[i] === "m") {
      strArray.push(m);
    }
    if (strSplit[i] === "M") {
      strArray.push(m);
    }
    if (strSplit[i] === "n") {
      strArray.push(n);
    }
    if (strSplit[i] === "N") {
      strArray.push(n);
    }
    if (strSplit[i] === "o") {
      strArray.push(o);
    }
    if (strSplit[i] === "O") {
      strArray.push(o);
    }
    if (strSplit[i] === "p") {
      strArray.push(p);
    }
    if (strSplit[i] === "P") {
      strArray.push(p);
    }
    if (strSplit[i] === "q") {
      strArray.push(q);
    }
    if (strSplit[i] === "Q") {
      strArray.push(q);
    }
    if (strSplit[i] === "r") {
      strArray.push(r);
    }
    if (strSplit[i] === "R") {
      strArray.push(r);
    }
    if (strSplit[i] === "s") {
      strArray.push(s);
    }
    if (strSplit[i] === "S") {
      strArray.push(s);
    }
    if (strSplit[i] === "t") {
      strArray.push(t);
    }
    if (strSplit[i] === "T") {
      strArray.push(t);
    }
    if (strSplit[i] === "u") {
      strArray.push(u);
    }
    if (strSplit[i] === "U") {
      strArray.push(u);
    }
    if (strSplit[i] === "v") {
      strArray.push(v);
    }
    if (strSplit[i] === "V") {
      strArray.push(v);
    }
    if (strSplit[i] === "w") {
      strArray.push(w);
    }
    if (strSplit[i] === "W") {
      strArray.push(w);
    }
    if (strSplit[i] === "x") {
      strArray.push(x);
    }
    if (strSplit[i] === "X") {
      strArray.push(x);
    }
    if (strSplit[i] === "y") {
      strArray.push(y);
    }
    if (strSplit[i] === "Y") {
      strArray.push(y);
    }
    if (strSplit[i] === "z") {
      strArray.push(z);
    }
    if (strSplit[i] === "Z") {
      strArray.push(z);
    }
  }
  return strArray.join("");
}